---
name: Что-то сломалось
about: Ошибка, странное поведение, пустой выпуск
labels: bug
---

## Что произошло

<!-- Что вы ждали и что получили. -->

## Как повторить

1.
2.

## Окружение

- версия (`git rev-parse --short HEAD`):
- Python (`python3 -V`):
- где крутится (VPS, локально, Docker):

## Логи

<!--
ВАЖНО: в логе и в выводе команд бывают ключи и адреса лент с токенами.
Перед вставкой прогоните файл через фильтр — он вырежет всё чувствительное:

    python3 digest.py scrub ~/.newsdigest/digest.log > safe.log
    python3 digest.py scrub --check safe.log      # код 1 = что-то осталось

Содержимое ~/.newsdigest/env не вставляйте никогда — там только секреты.
-->

```
сюда вычищенный лог
```

- [ ] Лог прогнан через `digest.py scrub`, токенов и паролей в тексте нет
